/**
 * pulp.events
 *
 * event constants for use with observable component
 * 
 */

(function( pulp, $ ) {
	"use strict";

	pulp.events = {
		TOC_LOADED : "TOC loaded!",
		CONTENT_LOADED : "Content loaded!"
	};

}( pulp, jQuery ));

